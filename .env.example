# Unsplash image search — public key, safe to expose at build time
VITE_UNSPLASH_ACCESS_KEY=

# Anthropic key is NOT stored here.
# Users enter it in the app; it is kept in sessionStorage only.
# See src/utils/aiKeys.ts
